<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.masson.alex.jsonplaceholder">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".application.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.splash.SplashActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.userlist.UserListActivity"
            android:label="@string/title_activity_user_list" />
        <activity
            android:name=".ui.userprofile.UserProfileActivity"
            android:label="@string/title_activity_user_profile" />
        <activity
            android:name=".ui.commentlist.CommentListActivity"
            android:label="@string/title_activity_comment_list"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".ui.photolist.PhotoListActivity"
            android:label="@string/title_activity_photo_list"
            android:theme="@style/AppTheme"></activity>
    </application>

</manifest>